#include <iostream>

int main() {
    int n;
    std::cin >> n;

    for (int i = 0; i < n; i++)
    {
        int mFront, mSize, mCap, last, correction;
        std::cin >> mFront >> mSize >> mCap >> last >> correction;
        if (mSize > mCap || last >= mCap || mFront >= mCap || )
    }
}